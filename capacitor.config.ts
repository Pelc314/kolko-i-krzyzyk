import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maciejpelcapps.kolkoikrzyzyk',
  appName: 'kolko-i-krzyzyk',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
